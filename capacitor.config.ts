import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.slagricola.checadoresapp',
  appName: 'SL_Checadores_App',
  webDir: 'www'
};

export default config;
